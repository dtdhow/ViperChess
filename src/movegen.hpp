// movegen.hpp
#pragma once
#include "board.hpp"

class MoveGen {
public:
    // static void generate_pawn_moves(const Board& board, std::vector<Move>& moves);
    // static void generate_king_moves(Square sq, Bitboard allies, Bitboard enemies, std::vector<Move>& moves);
    // ... other pieces
};